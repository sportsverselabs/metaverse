# tests/

Automated tests. **Empty for now** — there is no runtime code to test in Phase 0.

When code arrives (Phase 1+):
- Add a test for each new module.
- Keep tests portable (no machine-specific paths; no real secrets — use fakes/mocks).
- After a successful run, record it in `../CURRENT_STATUS.md` → "Last Successful Test".

The test framework will be chosen alongside the tech stack
(`../OWNER_ACTION_REQUIRED.md`, Action 2).
